# Core pipeline: STAC search, COG reads, indices, raster/vector outputs. These are the package's
# dependencies; the tools to run the notebook are in requirements-jupyter.txt.
# Python 3.12. Exact versions are frozen in requirements.lock.txt after install.
pystac-client>=0.8
planetary-computer>=1.0
odc-stac>=0.3.10
rioxarray>=0.17
rasterio>=1.4
xarray>=2024.6
numpy>=1.26,<3
pandas>=2.2
geopandas>=1.0
shapely>=2.0
pyproj>=3.6
scipy>=1.13
matplotlib>=3.8
folium>=0.17
rio-cogeo>=5.3
requests>=2.32
netCDF4>=1.6
